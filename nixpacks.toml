# Railway Nixpacks Configuration
[phases.setup]
nixPkgs = ["python313"]

[phases.install]
cmds = ["cd libslmgen && pip install -r requirements.txt"]

[start]
cmd = "cd libslmgen && uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}"
